{
  "$schema" : "http://json-schema.org/draft-07/schema#",
  "type" : "object",
  "required" : [ "enabled", "logError" ],
  "properties" : {
    "enabled" : {
      "type" : "boolean",
      "description" : "Enable request validation against the specification."
    },
    "logError" : {
      "type" : "boolean",
      "description" : "Log error message if validation error occurs."
    }
  }
}
